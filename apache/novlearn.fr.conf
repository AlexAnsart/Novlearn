<VirtualHost *:80>
    ServerName novlearn.fr
    ServerAlias www.novlearn.fr
    DocumentRoot /var/www/novlearn

    <Directory /var/www/novlearn>
        AllowOverride All
        Require all granted
    </Directory>

    # Exclure .well-known pour la validation Let's Encrypt (AVANT tout le reste)
    Alias /.well-known /var/www/novlearn/.well-known
    <Directory /var/www/novlearn/.well-known>
        AllowOverride None
        Require all granted
    </Directory>

    # Configuration du reverse proxy
    ProxyPreserveHost On
    ProxyRequests Off
    
    # Next.js API routes (port 3000)
    ProxyPass /api/admin http://localhost:3000/api/admin
    ProxyPassReverse /api/admin http://localhost:3000/api/admin

    # Backend FastAPI prod (port 8010)
    ProxyPass /api http://localhost:8010/api
    ProxyPassReverse /api http://localhost:8010/api

    ProxyPass /health http://localhost:8010/health
    ProxyPassReverse /health http://localhost:8010/health
    
    # Frontend Next.js prod (port 3000)
    ProxyPass / http://localhost:3000/
    ProxyPassReverse / http://localhost:3000/

    ErrorLog ${APACHE_LOG_DIR}/error_novlearn.log
    CustomLog ${APACHE_LOG_DIR}/access_novlearn.log combined
    
    # Redirection HTTPS (exclure .well-known)
    RewriteEngine on
    RewriteCond %{REQUEST_URI} !^/.well-known/acme-challenge/
    RewriteCond %{SERVER_NAME} =www.novlearn.fr [OR]
    RewriteCond %{SERVER_NAME} =novlearn.fr
    RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [END,NE,R=permanent]
</VirtualHost>

<VirtualHost *:80>
    ServerName staging.novlearn.fr
    DocumentRoot /var/www/novlearn-staging

    <Directory /var/www/novlearn-staging>
        AllowOverride All
        Require all granted
    </Directory>

    ProxyPreserveHost On
    ProxyRequests Off

    # Backend FastAPI staging (port 8011)
    ProxyPass /api http://localhost:8011/api
    ProxyPassReverse /api http://localhost:8011/api

    ProxyPass /health http://localhost:8011/health
    ProxyPassReverse /health http://localhost:8011/health

    # Frontend Next.js staging (port 3001)
    # Force proxying Next.js static assets (CSS, JS chunks, fonts)
    ProxyPass /_next/ http://localhost:3001/_next/
    ProxyPassReverse /_next/ http://localhost:3001/_next/
    ProxyPass /favicon.ico http://localhost:3001/favicon.ico
    ProxyPassReverse /favicon.ico http://localhost:3001/favicon.ico
    ProxyPass /manifest.json http://localhost:3001/manifest.json
    ProxyPassReverse /manifest.json http://localhost:3001/manifest.json

    ProxyPass / http://localhost:3001/
    ProxyPassReverse / http://localhost:3001/

    ErrorLog ${APACHE_LOG_DIR}/error_novlearn_staging.log
    CustomLog ${APACHE_LOG_DIR}/access_novlearn_staging.log combined

    RewriteEngine on
    RewriteCond %{SERVER_NAME} =staging.novlearn.fr
    RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [END,NE,R=permanent]
</VirtualHost>
